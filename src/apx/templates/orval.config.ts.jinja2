import type { Config } from "orval";

export default {
  api: {
    input: "http://127.0.0.1:8000/api/openapi.json",
    output: {
      target: "./src/{{app_name}}/lib/api.ts",
      baseUrl: "/api",
      client: "react-query",
      httpClient: "fetch",
      prettier: true,
      override: {
        query: {
          useQuery: true,
          useSuspenseQuery: true,
        },
      },
    },
  },
} satisfies Config;