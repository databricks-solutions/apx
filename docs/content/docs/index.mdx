---
title: Getting Started
description: Build Databricks Apps with apx
---

import { File, Folder, Files } from "fumadocs-ui/components/files";

Welcome to the apx documentation. apx is a reliable, feature-full, human and LLM friendly development toolkit for building Databricks Apps.

## Quick Start

Install apx:

```bash
curl -fsSL https://databricks-solutions.github.io/apx/install.sh | sh
```

Initialize a new project:

```bash
apx init
```

This launches an interactive prompt that guides you through:

- Naming your app
- Selecting a Databricks profile
- Setting up AI assistant rules
- Selecting addons (UI, backend, AI assistants)

## Prerequisites

Before getting started, ensure you have:

- **Databricks CLI** - For deployment ([install](https://docs.databricks.com/dev-tools/cli/install.html))

## Documentation

<Cards>
  <Card title="CLI Reference" href="/docs/reference/cli">
    Complete reference for all apx commands including init, build, and dev
  </Card>
  <Card title="MCP Reference" href="/docs/reference/mcp">
    Model Context Protocol server for AI-assisted development
  </Card>
  <Card title="Features" href="/docs/features">
    Dev server, components CLI, local database, and more
  </Card>
</Cards>

## Project Structure

After initialization, your project will have this structure:

<div className="font-mono">
  <Files>
    <Folder name="charming-aurora" defaultOpen>
      <File name="package.json" icon="📦" />
      <File name="pyproject.toml" icon="🐍" />
      <File name="databricks.yml" icon="⚙️" />
      <Folder name="src" defaultOpen>
        <Folder name="charming_aurora" defaultOpen>
          <Folder name="__dist__">
            <File name="Built frontend assets" />
          </Folder>
          <Folder name="backend" defaultOpen>
            <File name="app.py" />
            <File name="models.py" />
            <File name="router.py" />
          </Folder>
          <Folder name="ui" defaultOpen>
            <Folder name="components" />
            <Folder name="routes" />
            <File name="main.tsx" />
          </Folder>
        </Folder>
      </Folder>
    </Folder>
  </Files>
</div>

Quick overview of the most important files and directories:

- `package.json`: Frontend dependencies and scripts. Almost no need to edit this file manually.
- `pyproject.toml`: Python package metadata. Almost no need to edit this file manually.
- `databricks.yml`: Databricks Bundle configuration for deployment.
- `src/`: Source code for the app.
- `src/<app_name>/ui/`: React + Vite frontend.
- `src/<app_name>/backend/`: FastAPI backend.

## Development Workflow

1. **Start the dev server**

   ```bash
   apx dev start
   ```

2. **Add UI components**

   ```bash
   apx components add button card
   ```

3. **Check your code**

   ```bash
   apx dev check
   ```

4. **Build for deployment**

   ```bash
   apx build
   ```

5. **Deploy to Databricks**
   ```bash
   databricks bundle deploy -p <your-profile>
   ```

## Stack

apx is built on modern, production-ready technologies:

| Layer    | Technology                  |
| -------- | --------------------------- |
| Backend  | Python, FastAPI, Pydantic   |
| Frontend | React, TypeScript, Vite     |
| UI       | shadcn/ui, Tailwind CSS     |
| Runtime  | Bun, uv                     |
| Database | SQLModel, PGlite (optional) |

## Next Steps

<Cards>
  <Card title="CLI" href="/docs/reference/cli">
    Learn all available commands
  </Card>
  <Card title="MCP Server" href="/docs/reference/mcp">
    Set up AI assistant integration
  </Card>
  <Card title="Features" href="/docs/features">
    Explore dev server, components, and database
  </Card>
</Cards>
